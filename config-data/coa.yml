server:
  port: ${random.int(6000,6100)}
  servlet:
    context-path: /coa
spring:
  datasource:
      url: jdbc:postgresql://localhost:5432/dbControlAcademico
      username: controlUser
      password: 1234
      driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update